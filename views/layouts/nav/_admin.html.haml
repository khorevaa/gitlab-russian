%ul.nav.nav-sidebar
  = nav_link(controller: :dashboard, html_options: {class: 'home'}) do
    = link_to admin_root_path, title: "Статистика" do
      = icon('dashboard fw')
      %span
        Обзор
  = nav_link(controller: :projects) do
    = link_to admin_namespaces_projects_path, title: 'Проекты', data: {placement: 'right'} do
      = icon('cube fw')
      %span
        Проекты
  = nav_link(controller: :users) do
    = link_to admin_users_path, title: 'Пользователи', data: {placement: 'right'} do
      = icon('user fw')
      %span
        Пользователи
  = nav_link(controller: :groups) do
    = link_to admin_groups_path, title: 'Группы', data: {placement: 'right'} do
      = icon('group fw')
      %span
        Группы
  = nav_link(controller: :deploy_keys) do
    = link_to admin_deploy_keys_path, title: 'Ключи развертывания', data: {placement: 'right'} do
      = icon('key fw')
      %span
        Ключи развертывания
  = nav_link(controller: :logs) do
    = link_to admin_logs_path, title: 'Журналы', data: {placement: 'right'} do
      = icon('file-text fw')
      %span
        Журналы
  = nav_link(controller: :broadcast_messages) do
    = link_to admin_broadcast_messages_path, title: 'Широковещательные сообщения', data: {placement: 'right'} do
      = icon('bullhorn fw')
      %span
        Сообщения
  = nav_link(controller: :hooks) do
    = link_to admin_hooks_path, title: 'Веб-хуки', data: {placement: 'right'} do
      = icon('external-link fw')
      %span
        Веб-хуки
  = nav_link(controller: :background_jobs) do
    = link_to admin_background_jobs_path, title: 'Фоновые задания', data: {placement: 'right'} do
      = icon('cog fw')
      %span
        Фоновые задания

  = nav_link(controller: :applications) do
    = link_to admin_applications_path, title: 'Приложения', data: {placement: 'right'} do
      = icon('cloud fw')
      %span
        Приложения

  = nav_link(controller: :services) do
    = link_to admin_application_settings_services_path, title: 'Шаблоны служб', data: {placement: 'right'} do
      = icon('copy fw')
      %span
        Шаблоны служб

  = nav_link(controller: :abuse_reports) do
    = link_to admin_abuse_reports_path, title: "Отчеты о проблемах" do
      = icon('exclamation-circle fw')
      %span
        Отчеты о проблемах
        %span.count= AbuseReport.count(:all)

  = nav_link(controller: :application_settings, html_options: { class: 'separate-item'}) do
    = link_to admin_application_settings_path, title: 'Настройки', data: {placement: 'right'} do
      = icon('cogs fw')
      %span
        Настройки
