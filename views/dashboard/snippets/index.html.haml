- page_title    "Фрагменты"
- header_title  "Фрагменты", dashboard_snippets_path

= render 'dashboard/snippets_head'

.nav-block
  .controls
    = link_to new_snippet_path, class: "btn btn-new", title: "Новый фрагмент" do
      = icon('plus')
      Новый фрагмент

  .nav-links.snippet-scope-menu
    %li{ class: ("active" unless params[:scope]) }
      = link_to dashboard_snippets_path do
        Всем
        %span.badge
          = current_user.snippets.count

    %li{ class: ("active" if params[:scope] == "are_private") }
      = link_to dashboard_snippets_path(scope: 'are_private') do
        Приватный
        %span.badge
          = current_user.snippets.are_private.count

    %li{ class: ("active" if params[:scope] == "are_internal") }
      = link_to dashboard_snippets_path(scope: 'are_internal') do
        Внутренний
        %span.badge
          = current_user.snippets.are_internal.count

    %li{ class: ("active" if params[:scope] == "are_public") }
      = link_to dashboard_snippets_path(scope: 'are_public') do
        Публичный
        %span.badge
          = current_user.snippets.are_public.count

= render 'snippets/snippets'

