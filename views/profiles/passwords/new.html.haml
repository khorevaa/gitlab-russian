- page_title    "Новый пароль"
- header_title  "Новый пароль"
%h3.page-title Установка нового пароля
%hr
= form_for @user, url: profile_password_path, method: :post, html: { class: 'form-horizontal '} do |f|
  %p.slead
    Пожалуйста, установите новый пароль, прежде чем продолжить.
    %br
    После успешной установки пароля вы будете перенаправлены на экран для входа.
  -if @user.errors.any?
    .alert.alert-danger
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  - unless @user.password_automatically_set?
    .form-group
      = f.label :current_password, class: 'control-label'
      .col-sm-10= f.password_field :current_password, required: true, class: 'form-control'
  .form-group
    = f.label :password, class: 'control-label'
    .col-sm-10= f.password_field :password, required: true, class: 'form-control'
  .form-group
    = f.label :password_confirmation, class: 'control-label'
    .col-sm-10
      = f.password_field :password_confirmation, required: true, class: 'form-control'
  .form-actions
    = f.submit 'Установите новый пароль', class: "btn btn-create"
